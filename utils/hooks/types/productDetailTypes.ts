export interface ButtonsProps {
    buttonBackgroundColor: string;
    buttonText: string;
    marginTop: number;
    onPress?: () => void;
    borderWidth?: Boolean;
    textColor?: string;
}

export interface ProductDetails {
    products?: any
}